# Backend API base URL (empty = same origin / use Vite proxy in dev)
# In production on Vercel, set this to your backend URL:
# VITE_API_BASE_URL=https://your-backend.railway.app
VITE_API_BASE_URL=

# Optional API key (matches backend API_KEY setting)
VITE_API_KEY=dev_api_key_change_in_production
